import initialState from "../initialState";
import * as actionTypes from "../actions";

export default function companyReducer(state = initialState.user, action) {
  switch (action.type) {
    default:
      return state;
  }
}
